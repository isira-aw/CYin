package com.example.RegistrationLoginPage;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication  // scans all subpackages automatically
public class RegistrationLoginPageApplication {
    public static void main(String[] args) {
        SpringApplication.run(RegistrationLoginPageApplication.class, args);
    }
}
